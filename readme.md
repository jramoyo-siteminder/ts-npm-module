Sample NPM module written in TypeScript

**Requires** `tsc` to be installed

### Install
* `yarn install` 
* or `npm install`

### Clean
* `yarn run clean` 
* or `npm run clean`

### Test
* `gulp test`
* `gulp` (test)